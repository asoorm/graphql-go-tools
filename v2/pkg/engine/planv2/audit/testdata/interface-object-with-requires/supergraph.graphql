type Query {
  users: [NodeWithName!]!
  anotherUsers: [NodeWithName]
}

interface NodeWithName {
  id: ID!
  name: String
  username: String
}

type User implements NodeWithName {
  id: ID!
  name: String
  age: Int
  username: String
}
