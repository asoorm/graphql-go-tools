\* TLC config -- INSTANCE 1: Section 7.1 partial union with value-type members.
\* Run: java -cp tla2tools.jar tlc2.TLC -deadlock PlannerSearch.tla -config PlannerSearch.cfg
SPECIFICATION Spec

CONSTANTS
    Nodes      <- PU_Nodes
    Edges      <- PU_Edges
    Weights    <- PU_Weights
    RootNode   <- PU_Root
    Goals      <- PU_Goals
    ExpectedPi <- PU_ExpectedPi

INVARIANTS
    TypeOK
    SoundnessInv
    OptimalityInv

PROPERTIES
    EventuallyPlans
