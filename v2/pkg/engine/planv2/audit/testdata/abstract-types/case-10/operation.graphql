query ($title: Boolean = false) {
  products {
    id
    reviews {
      product {
        id
        ... on Book @skip(if: $title) {
          title
        }
        ... on Magazine {
          sku
        }
      }
    }
  }
}
