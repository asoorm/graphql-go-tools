extend schema
      @link(
        url: "https://specs.apollo.dev/federation/v2.3"
        import: ["@key", "@external"]
      )

    type Product @key(fields: "id") {
      id: ID!
      name: String!
    }

    type Query {
      products: [Product!]!
    }
