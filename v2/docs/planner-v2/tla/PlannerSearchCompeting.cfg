\* TLC config -- INSTANCE 3: competing derivations (min-selection stressor).
\* Node "fx" has two incoming derivations (direct f=1001 vs jump detour f=2012);
\* EXTRACT-MIN must settle it at 1001.  First-writer-wins would admit 2012.
\* Run: java -cp tla2tools.jar tlc2.TLC -deadlock PlannerSearch.tla -config PlannerSearchCompeting.cfg
SPECIFICATION Spec

CONSTANTS
    Nodes      <- CD_Nodes
    Edges      <- CD_Edges
    Weights    <- CD_Weights
    RootNode   <- CD_Root
    Goals      <- CD_Goals
    ExpectedPi <- CD_ExpectedPi

INVARIANTS
    TypeOK
    SoundnessInv
    OptimalityInv

PROPERTIES
    EventuallyPlans
